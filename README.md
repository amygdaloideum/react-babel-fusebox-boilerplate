# React babel fusebox boilerplate
A super simple hello world application to quickly get started with react.

## Get started
* Clone this repo.
* run `npm i` in the root folder.
* run `npm run dev` to start a dev-server with hot-reload.